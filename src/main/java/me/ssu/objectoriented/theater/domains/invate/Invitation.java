package me.ssu.objectoriented.theater.domains.invate;

import java.time.LocalDateTime;

/**
 * 1th Step, 이벤트 당첨자에게 발송되는 초대장을 구현.
 *  공연을 관람할 수 있는 초대일자(when)
 */
public class Invitation {
    private LocalDateTime when; // 초대일자
}
